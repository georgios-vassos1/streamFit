% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/online_super_learner.R
\name{update.online_sl}
\alias{update.online_sl}
\title{Update an `online_sl` object with a new observation}
\usage{
\method{update}{online_sl}(object, x, y, ...)
}
\arguments{
\item{object}{An `online_sl` object produced by [online_sl_fit()].}

\item{x}{Numeric vector of length `p`. Feature vector for the new
observation.}

\item{y}{Scalar response.}

\item{...}{Ignored.}
}
\value{
An updated `online_sl` object.
}
\description{
Performs one online step: computes base-learner predictions, updates the
ensemble weights via SGD, and (if supported) updates the base learners.
}
\examples{
\donttest{
set.seed(1)
n <- 400; p <- 4
X     <- cbind(1, matrix(rnorm(n * (p - 1)), n, p - 1))
beta0 <- c(-0.5, 1, -1, 0.5)
y     <- rbinom(n, 1, 1 / (1 + exp(-X \%*\% beta0)))

lib <- list(
  rls_glm = make_learner(
    fit     = function(X, y, family, ...) rls_glm_fit(X, y, family = family),
    predict = function(model, newdata, ...) predict(model, newdata),
    update  = function(model, x, y, family, ...) update(model, x = x, y = y)
  )
)
osl <- online_sl_fit(X[1:300, ], y[1:300], family = binomial(), library = lib)
osl <- update(osl, x = X[301, ], y = y[301])
}

}
\seealso{
[online_sl_fit()]
}
