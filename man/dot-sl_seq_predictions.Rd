% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/super_learner.R
\name{.sl_seq_predictions}
\alias{.sl_seq_predictions}
\title{Build prequential (one-step-ahead) out-of-sample predictions}
\usage{
.sl_seq_predictions(X, y, family, library, n_min, lnames)
}
\arguments{
\item{X}{Numeric matrix `n x p`.}

\item{y}{Numeric vector of length `n`.}

\item{family}{An R `family` object.}

\item{library}{Named list of `sl_learner` objects.}

\item{n_min}{Integer. Burn-in size; first model fit uses rows `1:n_min`.}

\item{lnames}{Character vector of learner names (length `L`).}
}
\value{
Numeric matrix `n x L` with `NA` in rows `1:n_min`.
}
\description{
For each time \eqn{t > n_{\min}}, trains each base learner on observations
\eqn{1,\dots,t-1} and records its prediction at \eqn{t} **before** seeing
\eqn{y_t}.  Rows \eqn{1,\dots,n_{\min}} are left as `NA` (burn-in).
}
\details{
Learners that expose an `update` function are advanced one step at a time
(O(n) total work).  Learners without `update` are re-fitted from scratch
on the growing prefix at each step (O(n^2) fits); for large `n`, supplying
an online learner is strongly preferred.
}
\keyword{internal}
